package teste;

public class ExecutaTeste {

	public static void main(String[] args) {
		//Dado um array
		/*int [] inteiros = {2,1,2};
		
		for (int i = 0; i < inteiros.length; i++) {
			if (inteiros[i] > Math.floorDiv(inteiros.length, 2)) {
				System.out.println(inteiros[i]);
			}
		}
		
		int a = array[0] + array[array.length-1];
		int b = array[1] + array[array.length-2];
		
		if (a > b) {
			System.out.println(a);
		} else {
			System.out.println(b);
		}
		
		int [] array = {1, 4, 3, 2};
		int tam = Math.floorDiv(array.length,2);
		Integer[] aux = new Integer[tam];
		int tamArray = array.length-1;
		
		for (int i = 0; i < array.length; i++) {
			if (aux.length > i) {
				aux[i] = array[i] + array[tamArray-i];
			} else {
				break;
			}
		}
		int resposta = 0;
		for (int i = 0; i < aux.length;) {
			if (aux[i] > aux[aux.length-1]) {
				resposta = aux[i];
				break;
			} else {
				resposta = aux[aux.length-1];
				break;
			}
		}
		
		//System.out.println(Math.floorDiv(array.length,2));*/
		
		String password = "teste"; //Pode ter 19 combinações
		

	}

}
